<Window x:Class="EditarePDF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EditarePDF"
        mc:Ignorable="d"
        Title="EditarePDF" Height="700" Width="1100" Background="#FAFAFA">
    <DockPanel>
        <!-- Top Toolbar -->
        <Border DockPanel.Dock="Top" Background="#2D3748" Padding="10">
            <DockPanel>
                <TextBlock Text="EditarePDF" Foreground="White" FontSize="18" FontWeight="SemiBold" DockPanel.Dock="Left" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <StackPanel.Resources>
                        <!-- Subtle shadow resource used by buttons -->
                        <DropShadowEffect x:Key="BtnShadow" BlurRadius="8" ShadowDepth="1" Opacity="0.25" Color="#000000"/>

                        <!-- Base button style -->
                        <Style TargetType="Button">
                            <Setter Property="Margin" Value="5"/>
                            <Setter Property="Padding" Value="14,10"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="#4A5568"/>
                            <Setter Property="BorderBrush" Value="Transparent"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="SnapsToDevicePixels" Value="True"/>
                            <Setter Property="FontSize" Value="16"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border CornerRadius="8" Background="{TemplateBinding Background}" Effect="{StaticResource BtnShadow}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#5A677A"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#2C5282"/>
                                                <Setter Property="RenderTransform">
                                                    <Setter.Value>
                                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Opacity" Value="0.6"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>

                        <!-- Accent style for primary actions -->
                        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Setter Property="Background" Value="#2C7A7B"/>
                            <Setter Property="FontSize" Value="16"/>
                        </Style>
                    </StackPanel.Resources>
                    <Button Content="Deschide PDF" Click="OpenPdf_Click"/>
                    <Button Content="Radieră (Alb)" Click="Eraser_Click"/>
                    <Button Content="Procesează OCR" Click="PrepareForOcr_Click" Style="{StaticResource PrimaryButton}"/>
                    <Button Content="Export Word" Click="ExportToWord_Click"/>
                    <Button Content="Export Excel" Click="ExportToExcel_Click"/>
                    <Button Content="Export PowerPoint" Click="ExportToPowerPoint_Click"/>
                    <Button Content="Export Imagine" Click="ExportToImage_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main Content Area -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Sidebar: Pages Thumbnails -->
            <Border Grid.Column="0" Background="#EDF2F7" BorderBrush="#E2E8F0" BorderThickness="0,1,1,0">
                <DockPanel>
                    <TextBlock Text="Pagini:" FontWeight="SemiBold" Foreground="#2D3748" Padding="12" DockPanel.Dock="Top"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="PagesList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Margin="10" Orientation="Vertical">
                                        <Border Padding="6" Background="#E3E3E3" BorderBrush="#CBD5E0" BorderThickness="1" CornerRadius="6">
                                            <Image Source="{Binding Thumbnail}" Stretch="Uniform" Width="240"/>
                                        </Border>
                                        <TextBlock Text="{Binding PageNumber, StringFormat=Pagina {0}}"
                                                   Foreground="#4A5568"
                                                   FontSize="12"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,6,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- Editor Surface -->
            <Border Grid.Column="1" Margin="10" Background="#F5F5F5" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                    <Grid x:Name="ContainerGrid" Margin="10">
                        <Image x:Name="PdfDisplayImage" Stretch="None" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                        <InkCanvas x:Name="EraserCanvas" Background="Transparent" EditingMode="None" HorizontalAlignment="Left" VerticalAlignment="Top"/>
                    </Grid>
                </ScrollViewer>
            </Border>
        </Grid>
    </DockPanel>
</Window>
